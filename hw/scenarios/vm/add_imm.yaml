engine: fast
map_mode: 1
program_base: 0
load:
  cw: hw/fixtures/control/cw.hex
  program: hw/fixtures/sram/add_imm.sram.hex
actions:
  - type: run
    max_steps: 200
expect:
  halted: true
  regs: [8, 0, 0, 0]
