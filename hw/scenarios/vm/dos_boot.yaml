kind: dos
actions:
  - type: command
    line: dir
  - type: command
    line: run HELLO.PLR
expect:
  output_contains:
    - stage1_kernel_ready
    - stage2_shell_ready
    - A>
    - HELLO
    - R0_7

