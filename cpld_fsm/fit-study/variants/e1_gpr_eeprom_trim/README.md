# E1 trim — 32/32 pin target

Same as [e1_gpr_eeprom](../e1_gpr_eeprom/) with **`q_a0..2` / `q_b0..2`** export only (6 pins).

Pin matrix row: **E1b-trim** — 24 total I/O, PASS 32/32.

Build: `.\scripts\build-variant.ps1 -Variant e1_gpr_eeprom_trim`
